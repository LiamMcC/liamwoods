<div class="mainCont">
    
    <h1>Categories</h1>
    
    <% @categories.each do |category| %>
    <div class="itemhols">
    
    <h1><%= category.title %></h1>
   <%=image_tag("golfswing.jpg", :class=> "welcomeIm") %>
    <%= category.image %>
    <p><%= category.description %></p>
    <p><a href="/category/<%= category.title %>" class="button"><%= category.title %></a></p>
    
    </div>
    <% end %>
    
    
</div>