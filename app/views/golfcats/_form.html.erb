<%= form_with(model: golfcat, local: true) do |form| %>
  <% if golfcat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(golfcat.errors.count, "error") %> prohibited this golfcat from being saved:</h2>

      <ul>
      <% golfcat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
